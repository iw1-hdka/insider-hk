{% extends 'base.html.twig' %}

{% block title %}FaqCategory{% endblock %}

{% block body %}
    <h1>FaqCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faq_category.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ faq_category.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_faq_category_index') }}">back to list</a>

    <a href="{{ path('app_back_faq_category_edit', {'id': faq_category.id}) }}">edit</a>

    {{ include('back/faq_category/_delete_form.html.twig') }}
{% endblock %}
